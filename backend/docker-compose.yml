services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: questengine
      POSTGRES_PASSWORD: questengine
      POSTGRES_DB: questengine
    ports:
      - "5432:5432"
    volumes:
      - dbdata:/var/lib/postgresql/data

volumes:
  dbdata:
